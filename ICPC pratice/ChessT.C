#include <iostream>
#include <vector>
using namespace std;

/* How to search for something
Foo *foo = std::find(std::begin(array), std::end(array), someObject);
// When the element is not found, std::find returns the end of the range
if (foo != std::end(array)) {
    cerr << "Found at position " << std::distance(array, foo) << endl;
} else {
    cerr << "Not found" << endl;
}*/

//decided to use sets instead of linked list.
//changed back to using linked list
//changed my mind again, now to vector
struct pool{
	vector<int> values;
	vector<int> lesser;
	vector<int> greater;

};
int main(){
	int players:
	int matches;
	cin<<players;matches;
	for (int i = 0; i < matches; i++){
		
	}
	return 0;
}